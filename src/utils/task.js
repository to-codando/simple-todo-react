import { observerFactory } from './observer.js'

export const observerCreateTask = observerFactory(null)
export const observerSelectedTask = observerFactory(null)
export const observerRemoveTask = observerFactory(null)